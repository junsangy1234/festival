TOUR_API_KEY=발급받은_관광공사_디코딩_인증키
APP_PORT=PORT

# ai-service (Part 6 · Gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
AI_SERVICE_PORT=8000
# 로컬에서 ai-service를 직접 띄울 때만 사용. docker compose는 값을 따로 주입한다.
JAVA_BACKEND_BASE_URL=http://localhost:8080

# 지도 표시용 Google Maps 브라우저 키 (Maps JavaScript API 사용 설정 필요)
GOOGLE_MAPS_API_KEY=

# CSV #9 문체부 지역축제 개최 계획 현황 (선택)
# FESTIVAL_HISTORY_CSV=file:./data/festival-history.csv

POSTGRES_DB=POSTGRES_DB
POSTGRES_USER=POSTGRES_DB
POSTGRES_PASSWORD=POSTGRES_DB
